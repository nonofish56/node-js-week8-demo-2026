# W8 課堂 demo（跟 Docker 裡的 PostgreSQL 對好了，不用改）

# 資料庫（Docker 起 PostgreSQL 也吃這三個值）
DB_USERNAME=student
DB_PASSWORD=student666
DB_DATABASE=livefit_demo
DB_HOST=localhost
DB_PORT=5434

# ⚠️ 鐵律：永遠 false——結構一律走 Migration
DB_SYNCHRONIZE=false
DB_ENABLE_SSL=false

# Web
PORT=3000
LOG_LEVEL=debug
